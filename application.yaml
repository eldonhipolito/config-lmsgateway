spring:
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: *
            allowedHeaders: *
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
            
